import React from "react";
import { PageWrapper } from "@/utils";
import HeroSection from "./components/Hero-Section";

const page = () => {
  return (
    <PageWrapper>
      <HeroSection />
    </PageWrapper>
  );
};

export default page;
